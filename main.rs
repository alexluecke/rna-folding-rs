fn main() {
    let rna_string = "".to_string();
    for j in (0..n) {
        let mut i = j-1;
        while i > 0 {
            i -= 1;
        }
    }
}

fn beta(x: isize, y: isize, S: &str) -> usize {
    0
}
